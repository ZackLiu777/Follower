# GIT-ACTIONS.md

- 我们在每次需要进行前缀`gamma`之类的版本迭代升级的时候，都需要创建新的branch，新的branch名字遵循相同的前缀名
- 每次在扩展实现新的功能以及UI的时候，对于确定性的结果，我们都需要添加相应的单元测试，测试覆盖情况越多越好，最后我们的单元测试需要添加到ios-ci.yml中，此为必须通过的测试(除了允许失败的DB测试外)
- 我们只需要在本地运行编译通过以及功能测试，UI测试主要在github actions跑(绝对不要在本地跑UI测试)
