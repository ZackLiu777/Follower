# CLAUDE.md

本仓库用于开发一款本地优先的 Instagram / TikTok 数据跟踪与分析应用。项目目标是提供清晰、可维护、可测试、适合 Codex 协作的工程环境。

## 1. 项目目标

- 使用 SwiftUI 构建 iOS App。
- 使用 MVVM 作为主架构。
- 使用 GRDB + SQLite 作为主数据存储。
- 所有用户数据默认只保存在本地，不上传云端。
- Alpha 版本只实现基础功能，Premium 能力只保留接口与数据结构，不做完整商业化实现。
- UI 以简洁直观为主，默认使用 Liquid Glass 风格；若出现性能问题，可切换到非 Liquid Glass 版本，但布局与交互结构必须保持一致。

## 2. 允许的架构边界

- View 只能展示状态和触发交互，不得直接访问数据库。
- ViewModel 只负责状态、编排和轻量业务逻辑，不得直接处理复杂 I/O。
- Repository 是唯一的数据访问入口。
- Sync Engine 负责与外部 API 同步。
- Ingestion Service 负责把 API 返回的数据转换为内部模型。
- Aggregation Service 负责把原始事件转成 Snapshot 和 Metrics。
- 所有持久化读写必须经过 GRDB 层。

## 3. 数据分层约束

项目采用单库多表设计，不允许多个独立持久化容器并存。

必须保留以下语义层：

- Event：原始观测记录，append-only。
- Snapshot：面向 UI 的状态快照。
- Metric：面向分析和 Premium 的派生指标。

规则：

- Event 只能追加，不能就地修改原始含义。
- Snapshot 可以覆盖更新或 upsert。
- Metric 由后台聚合产生，不允许在 View 中实时计算。
- JSON 仅用于导出，不作为主存储格式。

## 4. UI 约束

- 以 SwiftUI 为唯一 UI 框架。
- UI 设计以简洁、直观、低认知负担为第一优先级。
- 支持两套主题风格：
  - Apple Native 风格
  - Instagram 风格
- Liquid Glass 仅作为视觉层，不得改变数据流与业务逻辑。
- 所有列表和图表必须考虑懒加载和性能开销。

## 5. Premium 约束

Alpha 版本只实现基础版功能，但必须预留 Premium 接口。

Premium 可扩展方向包括：

- 趋势预测
- 粉丝增长预测
- 活跃度分析
- 活跃粉丝留存率
- 粉丝来源地域分布
- 互动质量评分
- 长期趋势对比
- CSV / Excel 导出
- 本地 AI / 统计分析
- 更强的数据加密与同步能力

规则：

- Premium 相关数据结构可以先定义，但实现必须可延后。
- Alpha 版不得因为 Premium 逻辑而阻塞基础功能交付。
- 不允许在 UI 中硬编码“未来会实现”的行为分支。

## 6. 性能约束

- 所有数据库读取必须尽量带条件、分页和限制。
- 所有批量写入必须使用批处理，避免逐条写入。
- 时间序列数据必须考虑压缩、归档和保留策略。
- 后台聚合必须异步执行，不能阻塞主线程。
- SwiftUI 列表必须使用 Lazy 组件或等价策略。

## 7. 测试约束

每次新增或修改功能时，必须同时更新测试。

最低要求：

- 每个确定性计算函数必须有单元测试。
- Repository 查询逻辑必须有测试。
- Aggregation 逻辑必须有测试。
- Alpha 主流程必须有本地集成测试。
- 不允许删除已有测试，除非有明确替代方案。

## 8. 代码风格约束

- 命名要准确表达业务含义。
- 函数应尽量小而单一职责。
- 复杂函数必须拆分。
- 中等及以上复杂度函数需要注释其输入、输出和意图。
- 文件顶部应有文件级说明注释。
- 新文件应放入正确模块，不得堆积在单一目录中。

## 9. 变更流程

每次完成一个功能迭代时：

1. 修改实现代码。
2. 修改或新增测试。
3. 更新相关文档。
4. 本地运行 build 与测试。
5. 使用 git commit 提交，提交信息带版本号，例如：
   - v0.01-alpha
   - v0.02-alpha

## 10. 禁止事项

- 禁止 View 直接操作数据库。
- 禁止把业务逻辑塞进 View。
- 禁止在 UI 层做重计算。
- 禁止引入未经评估的长期不维护库。
- 禁止把敏感用户数据上传云端，除非后续版本明确引入并重新评审。
- 禁止在没有测试的情况下新增复杂逻辑。
- 禁止同时修改过多无关文件而不说明理由。

## 11. 默认工作方式

当 Codex 需要处理任务时，优先顺序如下：

1. 阅读相关 spec 和 ADR。
2. 定位对应模块。
3. 做最小必要改动。
4. 补测试。
5. 更新必要文档。
6. 验证 build 与测试通过。

## 12. 完成标准

一个任务只有在以下条件同时满足时才算完成：

- 功能实现符合 spec。
- 测试已补齐并通过。
- 没有破坏现有架构边界。
- 没有引入明显性能退化。
- 相关文档已更新。